FROM node:16-alpine

RUN apk add --no-cache python3 py3-pip
RUN npm install -g npm@latest

WORKDIR /app

CMD ["sh", "-c", "                                          \
  echo '********** Backend NPM installation... **********'  \
  && npm install                                            \
  && npm run start:dev"]
